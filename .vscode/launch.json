{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run with input",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "args": [
                "<",
                "${workspaceFolder}/in/${fileBasenameNoExtension}.in"
            ],
            "justMyCode": true
        }
    ]
}